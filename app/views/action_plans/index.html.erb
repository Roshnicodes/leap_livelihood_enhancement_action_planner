<% content_for :title, "Project Action Plan | LEAP" %>

<%= render "action_plans/workspace",
  page_title: "Project Action Plan",
  form_path: action_plans_path,
  update_form_path: nil,
  submit_path: action_plans_path,
  project_options: @project_options,
  selected_project: @selected_project,
  fco_options: @fco_options,
  selected_fco_id: @selected_fco_id,
  to_options: @to_options,
  selected_to_id: @selected_to_id,
  period_options: @period_options,
  selected_period: @selected_period,
  selected_period_month: @selected_period_month,
  month_display_pairs: @month_display_pairs,
  state_options: @state_options,
  selected_state: @selected_state,
  vertical_options: @vertical_options,
  selected_vertical: @selected_vertical,
  download_path: current_user.admin? ? download_action_plans_path(request.query_parameters) : nil,
  rows: @rows,
  project_ownership_lookup: @project_ownership_lookup,
  theme_count: @theme_count,
  project_ownership: @project_ownership,
  vertical_labels: nil,
  show_achievement: @show_achievement,
  show_submission: @show_submission,
  editable_targets: @editable_targets,
  show_admin_columns: current_user.admin?,
  allow_all_projects: true,
  existing_submission: @existing_submission,
  balance_warning: @balance_warning,
  empty_message: "Choose a project to review the full action plan and submit it for approval.",
  fco_view: @fco_view %>
